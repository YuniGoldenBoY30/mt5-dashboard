<VirtualHost *:80>
    ServerName trading.zenixtech.ai
    
    # Redirigir todo el tráfico HTTP a HTTPS
    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</VirtualHost>

<VirtualHost *:443>
    ServerName trading.zenixtech.ai

    # ── Configuración SSL (Reemplaza con tus rutas reales de Certbot/Let's Encrypt) ──
    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/trading.zenixtech.ai/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/trading.zenixtech.ai/privkey.pem
    
    # Headers de Seguridad Institucional
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set X-Content-Type-Options "nosniff"

    ProxyPreserveHost On

    # ── Proxy Inverso a la API Backend (Docker localhost:8000) ──
    # Esto permite que los VPS se conecten directamente a la API a través de Apache
    # sin tener que pasar por el frontend.
    ProxyPass /api http://127.0.0.1:8000/api
    ProxyPassReverse /api http://127.0.0.1:8000/api

    # ── Proxy Inverso al Frontend (Docker localhost:8082) ──
    ProxyPass / http://127.0.0.1:8082/
    ProxyPassReverse / http://127.0.0.1:8082/
    
    # Soporte para WebSockets (Telemetría en tiempo real)
    RewriteEngine On
    RewriteCond %{HTTP:Upgrade} websocket [NC]
    RewriteCond %{HTTP:Connection} upgrade [NC]
    RewriteRule ^/ws/(.*) ws://127.0.0.1:8000/ws/$1 [P,L]

    ErrorLog ${APACHE_LOG_DIR}/trading_zenixtech_error.log
    CustomLog ${APACHE_LOG_DIR}/trading_zenixtech_access.log combined
</VirtualHost>
